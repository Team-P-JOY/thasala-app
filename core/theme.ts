import { DefaultTheme } from "react-native-paper";
const theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    primary: "#804A9B",
    outline: "#ddd",
    surfaceVariant: "white",
  },
};

export { theme };
